import React from "react";

const LoadingSpinner = () => (
  <div className="d-flex justify-content-center mt-4">
    <i className="loader__icon fa fa-spinner fa-spin fa-3x text-primary" />
  </div>
);

export default LoadingSpinner;
